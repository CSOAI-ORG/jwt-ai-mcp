{
  "name": "jwt-ai-mcp",
  "description": "jwt-ai-mcp \u2014 Built by [MEOK AI Labs](https://meok.ai)",
  "version": "1.0.0",
  "tools": [
    {
      "name": "decode_jwt",
      "description": "Decode a JWT token and show header, payload, signature.",
      "parameters": {
        "type": "object",
        "properties": {
          "token": {
            "type": "string"
          }
        },
        "required": [
          "token"
        ]
      }
    },
    {
      "name": "validate_jwt",
      "description": "Validate JWT signature and expiration.",
      "parameters": {
        "type": "object",
        "properties": {
          "token": {
            "type": "string"
          },
          "secret": {
            "type": "string"
          }
        },
        "required": [
          "token"
        ]
      }
    },
    {
      "name": "generate_jwt",
      "description": "Generate a signed JWT token.",
      "parameters": {
        "type": "object",
        "properties": {
          "payload": {
            "type": "string"
          },
          "secret": {
            "type": "string"
          },
          "algorithm": {
            "type": "string"
          }
        },
        "required": [
          "payload",
          "secret"
        ]
      }
    },
    {
      "name": "inspect_claims",
      "description": "Inspect JWT claims \u2014 issuer, audience, expiration, custom claims.",
      "parameters": {
        "type": "object",
        "properties": {
          "token": {
            "type": "string"
          }
        },
        "required": [
          "token"
        ]
      }
    }
  ]
}
