# Jwt Ai MCP

> MEOK AI Labs — JWT token operations — decode, validate, generate, inspect

`jwt-ai-mcp` is a Model Context Protocol (MCP) server by MEOK AI Labs.

## Install

```bash
pip install jwt-ai-mcp
```

Or run without installing:

```bash
uvx jwt-ai-mcp
```

## Tools

- `decode_jwt` — Decode a JWT token and show header, payload, signature.
- `validate_jwt` — Validate JWT signature and expiration.
- `generate_jwt` — Generate a signed JWT token.
- `inspect_claims` — Inspect JWT claims — issuer, audience, expiration, custom claims.
- `verify_expiration` — Check if JWT token is expired or still valid.

## Links

- Source: https://github.com/CSOAI-ORG/jwt-ai-mcp
- PyPI: https://pypi.org/project/jwt-ai-mcp/
- MEOK AI Labs: https://meok.ai
- Council of AI: https://councilof.ai
